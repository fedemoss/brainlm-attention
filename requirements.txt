# Verified working set (Python 3.10). The pins matter: transformers 4.28 needs the
# older huggingface_hub, and accelerate must stay compatible with it.
torch
transformers==4.28.0
tokenizers==0.13.3
huggingface_hub==0.18.0
accelerate==0.20.3
numpy
h5py
